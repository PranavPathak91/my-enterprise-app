# Environment Configuration for Enterprise Backend

# Server Configuration
# Define the port on which the server will run
PORT=5000                           # Default development port

# Database Configuration
# MongoDB connection string with authentication and database name
MONGODB_URI=mongodb://localhost:27017/enterprise_app   # Local development database

# Security Configuration
# JSON Web Token (JWT) secret for authentication
# IMPORTANT: Use a long, random, and unique string in production
JWT_SECRET=your_jwt_secret_here     # Replace with a secure random string
# Add JWT-specific configurations
JWT_SECRET=your_very_long_and_complex_secret_key_here
JWT_EXPIRES_IN=1d  # Token expiration (1 day)

# Frontend URL for CORS configuration
FRONTEND_URL=http://localhost:3000

# Application Environment
# Determines the behavior and verbosity of the application
NODE_ENV=development                # Can be 'development', 'production', or 'test'
